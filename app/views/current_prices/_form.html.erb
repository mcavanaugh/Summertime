<%= form_for(@current_price) do |f| %>
  <% if @current_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_price.errors.count, "error") %> prohibited this current_price from being saved:</h2>

      <ul>
      <% @current_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :symbol %><br />
    <%= f.text_field :symbol %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
